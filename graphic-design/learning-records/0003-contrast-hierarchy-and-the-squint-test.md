# 0003 — L3 delivered: Contrast → Hierarchy, with the squint test made interactive

**Date:** 2026-06-28

## What happened
- **Lesson 03 delivered: Contrast & Hierarchy** ("what the eye sees first"). Framing
  chosen deliberately for this learner: **contrast is the tool, hierarchy is the goal**
  — a rules/systems spine, not "taste." Led with the diagram (tool → builds → goal),
  per his diagrams-first preference.
- **The value-vs-hue trap got its own moment** (flagged in
  [[0001-mission-and-prior-knowledge]] and [[0002-visuals-gap-and-crap-progress]]).
  Built the **squint test as a live interactive**: two cards, one relying on hue at
  near-equal value (collapses to mud when desaturated), one on value (survives). A
  button toggles greyscale+blur so he can *feel* "value is what survives." New reusable
  asset `assets/squint.js` (wires any `[data-squint]` button → `.squinting` on a
  `.squint-zone`) + specimen CSS in `assets/lesson.css`.
- **Reuse reinforced on purpose:** the hierarchy "after" specimen still uses L02
  proximity + alignment, with a callout naming that contrast *adds the ranking* the
  other two couldn't. Quiz is **interleaved** across L01–L03 (contrast strength, the
  value/squint idea, hierarchy, and "which lever first" → size/value over hue).
- **Procreate drill:** build flat → ranked, then *squint-test it* (drop saturation to
  zero in a Hue/Sat adjustment) to check the hierarchy holds in pure grey.

## Still outstanding (retention debt is accumulating)
- **Three lessons delivered, zero demonstrated.** No before/after has come back yet
  (L01 flyer, L02 contact card, now L03 ranked poster + squint). Next session, *before
  any new material*, get evidence: have him either report a drill or critique a real
  piece naming contrast/value/hierarchy. Until then, treat retention as unknown.
- **Glossary:** Contrast, Hierarchy, and Value (with squint test) already exist and the
  lesson adhered to them — nothing added. "Squint test" is a candidate dedicated term,
  but per glossary discipline ("add only once the user can use it correctly"), hold it
  until he demonstrates it.

## Next lesson (candidate L04)
With C.R.A.P. now covered (A&P in L02, C+R touched here), the spine points to **Gestalt
— why the eye groups what it sees** (proximity/similarity/closure/common region/
figure-ground), which *explains why* the arranging principles work. Alternative if he
wants to go deeper on a single element: **Type 101** or **Colour: value before hue**
(the squint test sets that one up perfectly). Let his returned drills decide.
