# Notes

## About the learner
- Background: MATLAB, R, Python (strong). Data analysis + ML is his day job.
- Has "vibe-coded" several web projects with AI but doesn't understand what gets built.
- Wants the *why* and *when*, not syntax memorisation. Goal = read & tweak, not write from scratch.

## Teaching preferences (from session 1)
- **Diagrams & maps first**, then detail. Lead every lesson with a visual.
- **Anchor in the FHA project** (real code he owns) + occasional clean outside examples.
- Lean on **analogies to R / Python / MATLAB** — interpreters, compilers, packages, environments map well.
- Keep lessons short — one tangible win each.

## Conventions
- **Workspace (project-independent):** `C:\Users\Amir\Downloads\learning\web-dev\`.
  Deliberately NOT inside any code repo. To resume: open Claude Code here, run `/teach`.
  (Downloads isn't backed up — suggest moving `learning\` into OneDrive if he wants it safe.)
- **Example anchor:** the FHA site at
  `C:\Users\Amir\OneDrive - Digital Health CRC Limited\Projects\fha\`.
  This is the *current* example — swappable for any project he's working on.
- Lessons in `lessons/NNNN-*.html`, share `assets/lesson.css`. Glossary is authoritative for terms.
- FHA brand accents used in the course styling: Navy `#28235C`, Pink `#E61C73`, Azure `#009CDE`.

## Open threads
- LIST RECEIVED (session 1) — a ~60-item full-stack list in 6 tiers. Decoded into
  `reference/the-list-decoded.html`, placed on the 3-bucket map + front/back/cross split.
  He already uses the core: React, TypeScript, Vite, npm, Git (+ HTML/CSS/JS). Knows Python.
  Reframe used: "not 60 things — ~6 categories of mostly interchangeable alternatives; over
  half is backend = recognize only."
- Candidate one-off lessons he can pull on demand: "What's Tailwind & should I use it?",
  "How a front end talks to a back end (REST)", "How a site goes live (Vercel/Netlify)".
- Backend stays out of scope (FHA is frontend-only) but named on the map for later.
